# Terminal RAG 프로젝트 개선 인사이트

> 분석 일자: 2026-02-08
> 대상: 백엔드(FastAPI + RAG Engine), 프론트엔드(Next.js 16), 테스트, 인프라 전체

---

## 현재 프로젝트 평가 요약

| 영역 | 점수 | 비고 |
|------|------|------|
| 아키텍처 | 8/10 | 모듈 분리 우수, Strategy/Factory 패턴 적절 |
| 코드 품질 | 8/10 | 타입 힌트, 구조화 로깅, docstring 충실 |
| 보안 | 5/10 | 인증 부재, CORS 과다 허용, pickle 위험 |
| 성능 | 6/10 | 블로킹 I/O, 캐싱 없음, 비동기 미활용 |
| 테스트 | 5/10 | 커버리지 약 24%, 핵심 모듈 미테스트 |
| 프론트엔드 | 7/10 | 모던 스택, 스트리밍 지원, 접근성 부족 |
| 인프라/CI | 3/10 | CI/CD·린팅·포매팅 구성 전무 |

---

## 1. 보안 (Security) — 가장 시급

### 1-1. 백엔드 API 인증 부재
현재 `/index`, `/ask`, `/config/system-prompt` 등 모든 백엔드 엔드포인트에 인증이 없습니다. 특히 시스템 프롬프트 변경 API가 무방비 상태라 프롬프트 인젝션 위험이 있습니다.

**제안**: FastAPI dependency로 JWT 또는 API Key 미들웨어를 추가하고, Next.js 프론트의 NextAuth 세션 토큰을 백엔드까지 전달하는 구조를 만들어야 합니다.

### 1-2. CORS 전체 허용
`api/main.py`에서 `allow_origins=["*"]`로 설정되어 있어, 외부 도메인에서 API를 자유롭게 호출할 수 있습니다.

**제안**: 프로덕션 환경에서는 프론트엔드 도메인만 허용하도록 환경 변수로 관리해야 합니다.

### 1-3. Pickle 역직렬화 위험
`faiss_store.py`에서 `pickle.load()`를 사용하는데, 신뢰할 수 없는 파일을 불러올 경우 임의 코드 실행 위험이 있습니다.

**제안**: JSON + NumPy 바이너리 포맷 조합으로 전환하거나, 최소한 `pickle`을 사용할 경우 파일 무결성 검증(해시 체크)을 추가해야 합니다.

### 1-4. 파일 경로 검증 없음
`api/routes/index.py`의 `load_file(request.file_path)`에서 입력 경로를 검증하지 않아 Path Traversal 공격에 취약합니다.

**제안**: `pathlib.Path.resolve()`로 정규화 후 허용 디렉토리 내인지 검증하는 로직이 필요합니다.

### 1-5. 프론트엔드 API 키 localStorage 저장
Gemini API 키가 브라우저 localStorage에 평문 저장됩니다. XSS 취약점 발생 시 탈취 가능합니다.

**제안**: API 키를 백엔드에서만 관리하고, 프론트엔드는 서버 프록시를 통해 LLM을 호출하는 구조로 변경하는 것이 이상적입니다.

---

## 2. 성능 최적화 (Performance)

### 2-1. 비동기 처리 미활용
현재 임베딩과 LLM 호출이 동기(blocking)로 처리됩니다. FastAPI는 async를 지원하므로, 이를 활용하면 동시 요청 처리 성능이 크게 향상됩니다.

**제안**: `asyncio`와 `run_in_executor`를 사용해 CPU-bound 작업(임베딩)과 I/O-bound 작업(LLM API 호출)을 분리 처리하세요.

### 2-2. 쿼리/응답 캐싱 없음
동일한 질문에 대해 매번 임베딩 → 검색 → LLM 호출 전체 파이프라인이 반복됩니다.

**제안**: Redis 또는 인메모리 캐시(예: `cachetools`)를 도입해 쿼리 임베딩과 검색 결과를 캐싱하면 반복 질문 응답 속도가 대폭 개선됩니다.

### 2-3. 시맨틱 청킹의 높은 비용
`semantic.py`가 문서의 모든 문장을 개별 임베딩합니다. 긴 문서에서는 수백 번의 임베딩 호출이 발생합니다.

**제안**: 문장 단위 대신 슬라이딩 윈도우 방식의 배치 임베딩이나, 일정 문장 수를 묶어서 임베딩하는 방식으로 비용을 줄일 수 있습니다.

### 2-4. 프론트엔드 채팅 히스토리 최적화 필요
Zustand + localStorage로 전체 채팅 내역을 관리하는데, 대화가 쌓이면 동기 localStorage 접근이 UI를 블로킹할 수 있습니다. ChatList 컴포넌트도 모든 메시지를 매번 재렌더링합니다.

**제안**: 메시지 가상화(virtualization), `React.memo` 적용, 그리고 일정 크기 이상이면 IndexedDB로 이관하는 전략을 고려하세요.

### 2-5. Ollama LLM 하드코딩된 파라미터
`generation/llm.py`에서 Ollama의 temperature(0.1)와 num_predict(1024)가 하드코딩되어 있어 설정으로 제어할 수 없습니다.

**제안**: `configs/default.yaml`의 `generation` 섹션에 `temperature`, `max_tokens` 항목을 추가하고 코드에서 참조하도록 변경하세요.

---

## 3. 기능 완성도 (Feature Completeness)

### 3-1. 문서 삭제 시 벡터 DB 정리 미구현
프론트엔드에서 문서를 삭제하면 파일시스템과 Prisma DB에서만 제거되고, Qdrant에 남아 있는 청크는 삭제되지 않습니다. 코드에도 `TODO: Qdrant에서 해당 문서의 청크도 삭제`라는 주석이 있습니다.

**제안**: 백엔드에 `DELETE /documents/{source}` 엔드포인트를 추가하고, Qdrant의 `delete` API로 해당 source 메타데이터를 가진 포인트를 일괄 삭제하는 로직을 구현하세요.

### 3-2. 다중 문서 채팅 미완성
UI에 "다중 문서 채팅 기능은 준비 중입니다"라는 플레이스홀더가 있는데, 실제 구현되지 않았습니다.

**제안**: `source_filter` 파라미터가 이미 단일 문서 필터링을 지원하므로, 이를 리스트로 확장하면 비교적 적은 수정으로 다중 문서 범위 지정이 가능합니다.

### 3-3. 대화 내역 서버 영속화 없음
채팅 히스토리가 브라우저 localStorage에만 저장되어, 브라우저 캐시를 지우거나 다른 기기에서 접속하면 모든 대화가 사라집니다.

**제안**: Prisma 스키마에 `ChatSession`과 `ChatMessage` 모델을 추가하고, 주요 대화를 서버에도 저장하는 옵션을 제공하세요.

### 3-4. 메타데이터 기반 필터링 부족
현재 검색 필터는 `user_id`와 `source_filter`(파일명)만 지원합니다.

**제안**: 날짜 범위, 파일 타입, 태그 등 추가 필터 조건을 Qdrant의 payload 필터에 결합하면 더 정밀한 검색이 가능합니다.

### 3-5. 스트리밍 요청 취소 불가
사용자가 긴 응답 생성 중 취소할 수 없습니다. 백엔드가 응답을 멈추지 않으면 프론트엔드가 영원히 로딩 상태에 빠질 수 있습니다.

**제안**: 프론트엔드에 `AbortController`를 추가하고, 타임아웃(예: 60초)과 "중단" 버튼을 구현하세요.

---

## 4. 코드 품질 (Code Quality)

### 4-1. 중복 코드 발견
`generation/prompt.py`의 75-76행에서 `context_str = ...` 할당이 중복됩니다.

### 4-2. 매직 넘버 다수
`ask.py`에서 `search_top_k = request.top_k * 3` (rerank 시) 같은 매직 넘버가 하드코딩되어 있습니다. Kiwi 최대 문장 길이 2000자 등도 마찬가지입니다.

**제안**: 상수를 별도 모듈이나 config에 정의하여 관리하세요.

### 4-3. 에러 복구(Retry) 로직 없음
LLM API 호출이나 Qdrant 연결 실패 시 재시도 없이 즉시 에러를 반환합니다.

**제안**: `tenacity` 라이브러리를 사용해 일시적 오류(네트워크 타임아웃, API rate limit 등)에 대한 지수 백오프 재시도를 추가하세요.

### 4-4. 프론트엔드 Error Boundary 없음
React Error Boundary가 없어서 컴포넌트 에러 발생 시 전체 UI가 빈 화면이 될 수 있습니다.

**제안**: 최상위 layout에 ErrorBoundary 컴포넌트를 추가하고, 핵심 영역(채팅, 문서 목록 등)별로도 개별 Error Boundary를 설정하세요.

---

## 5. 테스트 및 CI/CD

### 5-1. 테스트 커버리지 부족 (약 24%)
전체 소스 약 4,092줄 중 약 1,000줄만 테스트됩니다. 특히 핵심 모듈이 미테스트 상태입니다.

**미테스트 고위험 모듈:**
- `embedding/qdrant_store.py` (220줄) — 벡터 DB 연동
- `generation/llm.py` (208줄) — LLM 통합
- `embedding/faiss_store.py` (169줄) — 벡터 저장소
- `ingestion/parsers/` (173줄) — PDF/텍스트 파싱
- `retrieval/reranker.py` (92줄) — 결과 재정렬

**제안**: 위 모듈부터 우선 테스트를 작성하세요. Qdrant는 테스트 컨테이너(`testcontainers-python`), LLM은 mock으로 단위 테스트를 구성할 수 있습니다.

### 5-2. CI/CD 파이프라인 전무
GitHub Actions, pre-commit hooks, 린팅/포매팅 설정이 모두 없습니다.

**제안 (우선순위 순):**
1. `ruff`를 pyproject.toml에 추가 (린팅 + 포매팅)
2. `.pre-commit-config.yaml` 생성 (ruff, mypy)
3. `.github/workflows/ci.yml` — push/PR 시 테스트 자동 실행
4. pytest-cov로 커버리지 측정 및 임계값(예: 60%) 설정

### 5-3. E2E 통합 테스트 없음
문서 업로드 → 파싱 → 청킹 → 임베딩 → 검색 → 응답 생성까지의 전체 흐름을 검증하는 테스트가 없습니다.

**제안**: Docker Compose 기반 통합 테스트 환경을 구성하고, 최소한 핵심 플로우 1~2개의 E2E 테스트를 추가하세요.

---

## 6. 인프라 및 배포 (Infrastructure)

### 6-1. Docker Compose 헬스체크 없음
세 서비스(qdrant, api, ui) 모두 `healthcheck` 설정이 없어서, 의존 서비스가 아직 준비되지 않은 상태에서 연결을 시도할 수 있습니다.

**제안**: 각 서비스에 healthcheck를 추가하세요. 예를 들어 API 서비스는 `/health` 엔드포인트를, Qdrant는 `curl http://localhost:6333/healthz`를 사용할 수 있습니다.

### 6-2. 로그 영속화 및 모니터링 부재
구조화 로깅(structlog)은 잘 설정되어 있지만, Docker 환경에서 로그가 컨테이너 종료 시 사라집니다.

**제안**: 로그 볼륨을 마운트하거나, 중앙 로그 수집(ELK, Loki 등)을 고려하세요.

### 6-3. 프론트엔드 SQLite의 한계
Prisma + SQLite를 사용하는데, 동시 쓰기가 많아지면 SQLite의 write-lock 제한에 걸릴 수 있습니다.

**제안**: 사용자 수가 늘어나면 PostgreSQL로 마이그레이션하는 것이 좋습니다. Prisma는 `provider = "postgresql"`로 변경하면 됩니다.

---

## 7. UX/접근성 (Accessibility)

### 7-1. ARIA 레이블 및 키보드 내비게이션 부족
사이드바, 모달 다이얼로그, 아이콘 버튼 등에 ARIA 속성이 부족합니다. 모달이 포커스 트랩을 하지 않고, 스킵 네비게이션 링크도 없습니다.

### 7-2. 모바일 반응형 미최적화
데스크톱 우선 설계로, 모바일 환경에서의 사이드바 처리나 입력 영역 최적화가 부족합니다.

### 7-3. 에러/성공 상태의 색상 의존
빨간색 = 에러, 초록색 = 성공으로만 표시되어 색맹 사용자가 구분하기 어렵습니다.

**제안**: 색상과 함께 아이콘이나 텍스트 레이블을 병행 표시하세요.

---

## 8. 추가 개선 아이디어

### 아키텍처 확장
- **WebSocket 지원**: SSE 대신 양방향 통신으로 전환하면 실시간 상태 업데이트가 용이합니다
- **작업 큐**: 대용량 문서 인덱싱 시 Celery/RQ 등으로 백그라운드 처리
- **멀티 테넌트 강화**: 현재 user_id 기반이지만, 팀/조직 단위 격리도 고려

### RAG 품질 향상
- **청크 중복 제거**: 서로 다른 문서에서 유사한 청크가 검색되는 것을 방지
- **하이브리드 검색 가중치 자동 튜닝**: 현재 alpha=0.7 고정인데, 질문 유형에 따라 동적 조정
- **컨텍스트 윈도우 관리**: LLM에 전달하는 컨텍스트의 총 토큰 수를 모델 제한에 맞게 자동 조절
- **피드백 루프**: 사용자 만족도 데이터를 수집해서 검색/생성 품질을 점진적으로 개선

### 개발 경험(DX)
- **Makefile 추가**: `make test`, `make dev`, `make lint` 등 공통 태스크 간소화
- **conftest.py 생성**: 테스트 공통 fixture를 한 곳에서 관리
- **API 문서 자동화**: FastAPI의 OpenAPI 스펙을 활용한 클라이언트 코드 자동 생성

---

## 우선순위 로드맵

| 단계 | 작업 | 예상 효과 |
|------|------|-----------|
| **1단계 (즉시)** | CORS 제한, 경로 검증, pickle 대체 | 보안 위험 해소 |
| **1단계 (즉시)** | ruff + pre-commit 설정 | 코드 품질 기준선 확보 |
| **2단계 (1주)** | 백엔드 API 인증, 벡터 DB 문서 삭제 | 기능 완성도 향상 |
| **2단계 (1주)** | GitHub Actions CI, 핵심 모듈 테스트 추가 | 안정성 확보 |
| **3단계 (2주)** | 비동기 처리, 쿼리 캐싱 | 성능 개선 |
| **3단계 (2주)** | Error Boundary, AbortController | UX 안정성 |
| **4단계 (1개월)** | 대화 영속화, 다중 문서 채팅, 메타데이터 필터 | 기능 확장 |
| **5단계 (장기)** | PostgreSQL 마이그레이션, 모니터링, 피드백 루프 | 프로덕션 준비 |

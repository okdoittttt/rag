# Docker 환경 전용 설정
# 이 파일은 docker-compose 실행 시 사용됩니다.

# === Qdrant (Docker 네트워크 내부) ===
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# === API Server ===
API_HOST=0.0.0.0
API_PORT=8000

# === LLM Provider ===
# Gemini 사용 시 (Docker 외부에서 설정 필요)
# GOOGLE_API_KEY=your-api-key
# GOOGLE_MODEL=gemini-2.5-flash

# Ollama 사용 시 (Docker 호스트에서 실행 중인 경우)
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.1:8B

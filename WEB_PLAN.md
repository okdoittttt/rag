# Web UI 구현 계획

RAG 시스템을 위한 Next.js 기반 웹 인터페이스 구현 계획입니다.
`ui/` 디렉토리에 구축하며, TailwindCSS를 사용합니다.

---

## 📋 구현 단계

### Phase 1: 프로젝트 초기화 ✅
- [ ] `ui/` 디렉토리에 Next.js App Router 프로젝트 생성
- [ ] TailwindCSS, Lucide-React(아이콘) 설정
- [ ] Proxy 설정 (`next.config.js`) - API 서버(8000) 연동
- [ ] 기본 레이아웃 (Header, Main Container)

### Phase 2: 검색 및 채팅 인터페이스
- [ ] **Search Bar**: 질문 입력 컴포넌트
- [ ] **Chat Container**: 대화 목록 표시 영역
- [ ] **Message Bubble**:
    - 질문 (User)
    - 답변 (Bot) - Markdown 렌더링 지원
    - 로딩 인디케이터

### Phase 3: API 연동
- [ ] 리액트 훅 구현 (`useChat`, `useSearch`)
- [ ] **POST /ask** 연동
- [ ] **POST /search** 연동 (검색 전용 모드)
- [ ] **POST /index** 연동 (문서 업로드 파일 입력)

### Phase 4: 스트리밍 답변 (SSE)
- [ ] `EventSource` 또는 `fetch` 스트림 리더 구현
- [ ] 실시간 텍스트 타이핑 효과
- [ ] 실시간 참조 문서(Source) 표시

### Phase 5: 문서 관리 및 배포
- [ ] 문서 인덱싱 UI (텍스트/파일 업로드)
- [ ] Dockerfile 작성 (`Dockerfile.web`)
- [ ] `compose.yaml`에 서비스 추가 (`web` 서비스)

---

## 📁 디렉토리 구조 (예정)

```
ui/
├── app/
│   ├── page.tsx          # 메인 채팅 화면
│   ├── layout.tsx        # 글로벌 레이아웃
│   └── globals.css       # Tailwind 설정
├── components/
│   ├── chat/             # 채팅 관련 컴포넌트
│   │   ├── ChatList.tsx
│   │   ├── ChatMessage.tsx
│   │   └── ChatInput.tsx
│   ├── common/           # 공통 컴포넌트 (Button, Input 등)
│   └── layout/           # Header, Sidebar
├── lib/
│   ├── api.ts            # API 호출 함수
│   └── hooks/            # 커스텀 훅 (useChat)
└── next.config.ts        # 프록시 설정
```

---

## ⏱️ 예상 일정

| Phase | 예상 소요 |
|-------|----------|
| Phase 1 | 30분 |
| Phase 2 | 1-2시간 |
| Phase 3 | 1시간 |
| Phase 4 | 2시간 |
| Phase 5 | 1시간 |

---

*작성일: 2026-01-18*
